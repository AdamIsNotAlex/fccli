# Todo List

## Locked decisions

- `fccli <instrument> <timeframe>` renders one snapshot and exits; `-i` / `--interactive` starts the interactive TUI.
- Interactive mode requires both stdin and stdout to be TTYs. If either is not a TTY, it exits nonzero with an actionable recommendation to use snapshot mode; it never silently changes mode.
- The default provider is the case-sensitive lowercase name `binance`; v1 supports Binance Spot only and does not support Binance Futures.
- CLI parsing produces a provider-neutral `InstrumentSpec`. Binance performs local, pure canonicalization into `Instrument`; v1 does not query `exchangeInfo`.
- Symbol components are uppercased. Bare assets default to USDT: `btc` and `binance:btc` resolve to `BTCUSDT`; `btc/usdc`, `btc-usdt`, and `BTCUSDT` are accepted explicit forms. Under this locked grammar, any separator-free token not ending in `USDT`, including `BTCUSDC`, receives a `USDT` suffix.
- The normalized display pair is always `BASE/QUOTE` (for example `BTC/USDT`), independently of the opaque Binance transport symbol (`BTCUSDT`).
- Reject more than one colon, more than one pair separator, mixed `/` and `-`, empty components, whitespace, and non-ASCII-alphanumeric components.
- Supported timeframes are exactly `1s`, `1m`, `3m`, `5m`, `15m`, `30m`, `1h`, `2h`, `4h`, `6h`, `8h`, `12h`, `1d`, `3d`, `1w`, and `1M`; parsing is case-sensitive.
- Provider-neutral timestamps are Unix milliseconds.
- The chart contains a two-line source/symbol/OHLCV header, main candlestick pane, right-side price axis, volume pane, and UTC time axis. The shortcut/status footer is interactive-only.
- Rendering receives one explicit `RenderPolicy`, detected once from output capability and `NO_COLOR`; it selects deterministic green/red TTY styles or style-free monochrome bull=`█`, bear=`▓`, doji=`━` with the existing wick geometry.
- Snapshot output uses the current terminal dimensions minus one shell row. The 60×18 minimum applies to the effective rendered frame, so a snapshot TTY requires at least 60×19 physical cells. Non-TTY output uses an effective 120×36 frame and contains no ANSI escapes.
- Interactive frames smaller than the effective 60×18 minimum remain running and display only a resize instruction; no partial chart geometry is exposed.
- Interactive startup loads the latest 500 candles. Near the left history boundary, one serialized request loads up to 1000 older candles with checked `endTime = oldest_open_time - 1`.
- Binance history uses REST; Binance live updates use WebSocket. Both are exposed through provider-neutral candle and status events. The application alone owns `CandleSeries` and `ChartViewState`; network tasks emit immutable results/events. REST emits validated immutable `RestProvisionalOpen` candles only; `CandleSeries` alone recomputes provider-successor adjacency after every merge, promoting/demoting `RestProvisionalClosed` independently of page boundaries.
- Equal-`open_time` merge precedence uses the exact `FinalityAuthority { RestProvisionalOpen, RestProvisionalClosed, WsAuthoritativeOpen, WsAuthoritativeClosed }` table: `WsAuthoritativeClosed` cannot regress; WS beats REST; closed beats open within one provenance when closure is valid; within the same resulting state later acceptance wins. Structural REST closure is a series-level adjacency hint and never authoritative.
- Live startup/reconnect opens and continuously polls WS first. Every generation, including nonempty history, waits at most `FIRST_KLINE_HANDSHAKE_TIMEOUT = 10 s` for a first decoded kline before REST reconciliation can complete; event-at-deadline wins. `Some(confirmed)` keeps inclusive start at confirmed and uses that kline to establish the initial target; `None` uses it as start and initial target. During reconciliation, every WS candle mutation advances `revision` and sets `target_open_time = max(current_target_open_time, candle.open_time)`, including accepted or coalesced mutations racing any REST page; every ordered tagged batch registers its exact latest `ReconcileExpectation` before delivery.
- App publishes monotonic accepted watermark and proof-only matching `ReconcileAck` through the exact canonical awaitable latest-value APIs. Missing-middle publishes no ack; `RECONCILE_ACK_TIMEOUT = 10 s` with ack-at-deadline wins, otherwise exact `ReconcileAckTimeout` invalidates/purges/closes and enters Backoff. `Connected` waits until App proves contiguous accepted `CandleSeries` coverage through the latest registered `target_open_time` and `revision`, including targets advanced across multiple REST pages or by racing WS mutations.
- Tungstenite 0.30 automatic Pong handling is used; application code never emits a duplicate Pong. The socket owner continuously selects over read/flush progress, cancellation, and downstream readiness, using bounded keyed/coalescing candle output and bounded status/error output so a full application channel cannot stop multiple Ping/Pong progress. Production capacities are exactly `KEYED_CANDLE_CAPACITY = 1024`, `CONTROL_CAPACITY = 64`, `EMERGENCY_CONTROL_CAPACITY = 2`, and App-facing `MARKET_EVENT_CHANNEL_CAPACITY = 256`; test-injectable non-emergency capacities accept only `1..=65_536`, while emergency capacity is non-configurable, permanently reserved, and exactly two slots. Queue saturation uses those two slots for exactly one overflow issue plus `Backoff`: atomically invalidate the generation, purge every queued control and unacknowledged candle for it (including a queued `Connected`), close/discard the socket, and deliver the reserved pair in order as `generation: None` controls when capacity returns before starting the next generation. The reducer accepts that out-of-generation pair without reactivating stale data.
- WebSocket reconnect states are exactly `Connecting`, `GapSync`, `Connected`, `Backoff`, and `Stopped`. The exact closed API is `MarketEvent::Status { generation: Option<GapGeneration>, status: ConnectionStatus } | ReconcileBatch { generation: GapGeneration, revision: ReplayRevision, target_open_time: i64, candles: Vec<Candle> } | Candle { generation: GapGeneration, candle: Candle } | RecoverableError { generation: Option<GapGeneration>, error: ProviderError, rate_gate_deadline: Option<MonoInstant> } | TerminalError(ProviderError)`. Stream `Err` is reserved for supervisor/join/channel failure after no `TerminalError` can be delivered. Producers and the App consumer match `MarketEvent` exhaustively without wildcard; only the App reducer rejects stale/invalid generations. Status transitions, reconnect, reconciliation/backfill, and rate-limit waits/errors never clear already accepted `CandleSeries` data or reset the inspected viewport; only accepted series mutations apply the locked anchor-preserving view update. Reconnect backoff is exactly `1, 2, 4, 8, 16, 30, 30, …` seconds, resets only after acknowledged `Connected`, and wakes at the later of the checked backoff deadline and the current shared rate-gate deadline.
- One shared cancellation-aware Binance REST rate gate applies to initial, history, and gap requests and exposes a cloneable provider-neutral latest-deadline snapshot/watch handle to the history coordinator and supervisor; every wake rereads it, and receiver closure is terminal and typed. Valid checked delta-seconds for 429 or 418 extend but never shorten the gate. Missing/malformed/negative/overflowing 429 uses the injected fallback, exactly 30 seconds in production. Missing or invalid 418 records a process-lifetime blocker status and disables every automatic REST/history/reconnect retry for that provider process; only explicit process restart can resume. A valid 418 deadline remains retryable only after that deadline.
- Live-follow starts enabled. Moving left pauses it; reaching the latest boundary restores it. `End` returns to latest while preserving X zoom and Y state; `r` restores interactive-default X span, Y auto-scale, latest position, and live-follow.
- Keyboard controls are locked: `A/←` older, `D/→` newer, `W/↑` price window up, `S/↓` price window down, `h` X zoom in, `H` X zoom out, `v` Y zoom in, `V` Y zoom out, `q/Esc/Ctrl-C` quit. Lowercase and uppercase WASD both move; `h/H` and `v/V` remain case-sensitive.
- Keyboard X pan is `max(1, ceil(visible_count × 0.05))`; Y pan is 10% of price span. X/Y zoom-in multiplies range by `0.8`, zoom-out by `1.25`; integer X zoom rounds to nearest with ties away from zero.
- Snapshot initialization shows the latest `min(series_len, plot_width)`. Interactive empty data stays distinct; lengths `1..=9` initialize to `min(series_len, plot_width)`, and lengths `>= 10` initialize to `min(series_len, plot_width, max(10, ceil(plot_width / 2)))`. For every nonempty interactive series, `min_visible_count = min(series_len, plot_width, 10)`; apply this minimum after every keyboard or mouse X zoom and then reclamp all visibility bounds, so another zoom-in at the minimum is blocked. For chart-content width `W`, `gutter_width = if W >= 2 { 1 } else { 0 }`, `price_label_width = min(14, W.saturating_sub(gutter_width + 1))`, and `plot_width = W - gutter_width - price_label_width`. Header is exactly two rows, interactive footer exactly one row, snapshot footer zero rows; from remaining body height `H`, exactly `volume_height = clamp((H + 2) / 5, 3, H - 1)` and `main_plot_height = H - volume_height`. Width/height arithmetic is checked/saturating; infeasible sizes return a resize-only `LayoutPending` result with no chart rectangles.
- After that one-time App transition, resize never reinitializes: it preserves logical center/state and reclamps. All exact Y scaling, keyboard/mouse anchors, and status header behavior remain locked.
- Mouse behavior is locked: left-button plot drag pans X and Y, right price-axis drag scales Y, bottom time-axis drag scales X, and hover displays a crosshair plus nearest-candle UTC OHLCV. Rectangles use half-open containment; nearest-candle ties choose the earlier/left candle.
- Drag ownership is fixed by the left-button-down region and continues outside it with coordinates clamped to the frame. Plot content follows the pointer: rightward drag shows older indices and downward drag raises the viewed price center so the rendered candle content moves downward.
- Price-axis upward drag zooms in by `1 / 1.05^rows`; downward drag zooms out by `1.05^rows`. Time-axis rightward drag zooms in and leftward drag zooms out by the corresponding factor. Dragging suppresses the crosshair until release.
- Style-free output, including every non-TTY snapshot regardless of `NO_COLOR`, uses exact monochrome body glyphs bull=`█`, bear=`▓`, doji=`━` with the existing wick geometry; TTY color output may additionally use green/red styles.
- Persistent logical X anchors are identified by candle `open_time` or updated through an index-aware mutation mapping, so prepends and middle insertions before/inside/after the viewport preserve the same candle under each anchored column.
- Interactive event reduction uses the plan's sole canonical bounded two-phase reducer contract, including its complete intra-bucket total order, terminal short-circuit, resize-before-pointer rule, stale-generation rejection, same-time merge precedence, and per-source quota.
- Snapshot and interactive modes share the same provider-neutral domain models, view state, layout calculation, and custom Ratatui `Buffer` renderer.
- Rust baseline is Edition 2024 with exact toolchain 1.96.0. Supported platforms are Linux, macOS, and Windows.
- Automated tests use local REST/WS mocks, injected/manual time, deterministic terminal seams, injected library entry points, and no public Binance or real-terminal access. Real Binance and real-terminal access are explicit smoke checks only.
- Automated network tests compile/run under an enforced loopback-only transport mode in which production constructors are unavailable, bind mocks to literal loopback endpoints, disable redirects or reject every redirect target before connection, and include a deliberate direct-public-connect attempt blocked by the harness. CI uses an OS-level egress-deny boundary where supported and a separate compile-only production-network build; public hosts, DNS hostnames, and loopback-to-public redirects are rejected.
- V1 excludes indicators, drawing tools, trading, image export, persistent configuration, disk caching, multiple charts, additional providers, API keys, exchange metadata discovery, generic polling, synthetic candle aggregation, telemetry, compatibility shims, and retry systems beyond the specified REST/WS/backfill behavior.

## Sequential implementation chunks

Each chunk is independently reviewable and committable after its definition of done and listed deterministic validation pass. All items remain unchecked until implementation occurs.

### 1. Freeze the implementation contract

- [x] Confirm `docs/plan/init.plan.md` and this checklist agree on every locked decision above, exact generation-and-revision-tagged `MarketEvent`/ack API, generation-tagged live candles, the canonical reducer order and separate renderer display/final-error precedence, dependency order, ownership boundaries, dynamic reconnect watermark, bounded socket-owner buffering/Ping progress and out-of-generation saturation recovery, absolute deadline units, rate-gate observation/418 blocker policy, deterministic layout/view sequencing, chunk/registry ownership, and explicit exclusions.
- [x] Confirm ambiguous behavior is resolved before source work: non-TTY interactive error, footer by mode, complete symbol grammar including quote-only rejection, formatter-safe Unix-ms timestamps, `FinalityAuthority` as the sole stored finality, provisional REST versus series-owned adjacency versus authoritative WS finality, reconnect reset/rate-gate/status-specific `Retry-After` rules, exact snapshot/interactive initialization and global plot-width ceiling, 5% auto-Y padding, viewport rounding/anchors, mouse signs/ties/price anchor and outside-plot hover clearing, tiny/large price and compact-volume formatting, style-free direction glyphs, effective dimensions, terminal unwind/final-error accounting, durable validation ledger, and every original validation.
- Definition of done: no implementation behavior is left to be selected ad hoc during coding; the plan and todo use the same exact types/states/tables, and every task remains unchecked until implementation.

### 2. Create the reproducible application skeleton

- [x] Create binary package `fccli`, Edition 2024, `rust-version = "1.96"`, exact Rust 1.96.0 toolchain configuration, generated retained `Cargo.lock`, and only the chunk-2 module/test paths needed now, plus the target-specific Linux PTY harness dependency `nix = "=0.30.1"` with `default-features = false` and features `term`, `process`, and `fs`, using `openpty`/spawn so macOS/Windows all-target builds remain clean. The plan's final tree is an end-state inventory and MUST NOT pre-create files assigned to later chunks.
- [x] Pin the exact direct dependencies and minimal features listed by `init.plan.md` § Reproducible baseline, including the canonical cancellation-token dependency; do not maintain a second version/feature inventory here.
- [x] Define mutually exclusive Cargo features `test-transport` and `production-transport`, with `production-transport` the default used by ordinary `cargo run`. Under `test-transport`, production constructors/hosts are not compiled and the `fccli` binary remains buildable for Assert Cmd help/version/invalid-input tests without obtaining production transport. In this chunk validate only feature mutual exclusion, default selection, and successful test-/production-feature compilation; do not add a constructor compile-fail fixture before a real constructor exists.
- [x] Establish `src/main.rs`, `src/lib.rs`, `src/provider/mod.rs`, `src/chart/mod.rs`, support-module locations needed by the skeleton, `tests/feature_selection.rs`, and `tests/fixtures/`; keep `main.rs` limited to bootstrap, CLI dispatch, runtime startup, top-level reporting, and final process status. Do not create `tests/app_live_contract.rs` in this chunk; chunk 17 creates it with the real App/dispatcher contract.
- Definition of done: the empty architecture checks with Rust 1.96.0, exact pins resolve into the lockfile, feature mutual exclusion/default/API visibility and TLS/backend features are unambiguous, and no application logic or compatibility shim is hidden in bootstrap.

### 3. Define provider-neutral domain and events

- [x] Define non-circular typed error taxonomy for application, provider, payload, HTTP/status, invalid symbol, rate limit, timeout, transport, terminal, and rendering failures with provider/symbol/timeframe/operation/cause context and no raw payload dumps. `RecoverableError` and `TerminalError` are distinct event data; stream `Err` is only supervisor/join/channel failure.
- [x] Define only chunk-3 domain/event/value foundations, including exact `MonoInstant` and every absolute-deadline value used by `MarketEvent`/`RateGateState`; provider aliases/traits/channels, cancellation, `Clock`, scheduler/conversion/task seams belong to chunk 7. `RendererSnapshot` belongs to chunk 12 and dispatch belongs to chunk 17.
- [x] Store candle OHLC, unchanged base volume, open/close Unix-ms timestamps, and authority only; derive open/closed finality from `FinalityAuthority` and never store a second boolean/state. Reject non-finite numbers, negative volume, invalid timestamp order/body bounds, timestamps outside the shared `time::OffsetDateTime`-format/calendar/pagination-safe millisecond domain, and values outside the precisely defined chart-safe finite domain. Define REST input as provisional only and freeze the complete merge/adjacency rules for chunk 4 without performing series-level closure here.
- [x] Give `HistoryRequest` validated latest/older/gap constructors, checked timestamp subtraction/addition within the same formatter-safe domain, allowed limits/ranges, `start <= end`, inclusive watermark overlap, checked next-page `last_returned_open_time + 1` millisecond, and no-progress safeguards that do not use fixed timeframe duration for `1M`.
- [x] Freeze only domain-level validation and checked timestamp arithmetic needed by these values; do not introduce provider task/runtime seams or chart-state formulas in this chunk.
- Definition of done: domain tests include `MonoInstant`/deadline values; provider task behavior and `Clock` abstraction start in chunk 7.

### 4. Implement pure candle storage

- [x] Implement store-only `CandleSeries` with `VecDeque<Candle>`: initial replacement, older prepend, indexed/visible access, exact `open_time` lookup, oldest/newest timestamps, live upsert/append, arbitrary batch merge, and calendar-aware continuity query. It owns no end-of-history/no-progress latch and no view anchor, drag, or hover state.
- [x] Accept and stably sort unsorted input, preserving acceptance order among equal `open_time`; deduplicate overlaps, enforce strict chronological uniqueness, apply the exact four-state precedence, and after every mutation recompute provider-successor REST adjacency over the complete merged series, promoting or demoting provisional closure independently of HTTP pages.
- [x] Return exactly the canonical `MutationSummary { inserted, replaced, unchanged, old_to_new, resolved, empty_input, duplicate_only, no_progress }`, where `old_to_new: IndexMapping` and `IndexMapping` is exactly `Identity { len } | ShiftSuffix { len, from, delta: isize } | Explicit(Vec<usize>)` with `len()` and `map(old_index) -> Option<usize>`; every produced mapping resolves every valid prior index. Same-key newest upsert and newest append use allocation-free `Identity`; a single middle/prepend shift uses `ShiftSuffix` where applicable; only a general nonuniform batch may use `Explicit`. Appended predecessor detection checks the previous back element first. Keep `ResolvedMutation { open_time, final_index, kind }` and `MutationKind { Inserted, Replaced, Unchanged }`; consumers derive prepended/inserted ranges and appended/updated facts rather than storing a competing taxonomy, and all observable mutation semantics remain unchanged.
- Definition of done: deterministic store tests cover empty, one/current-open candle, malformed, out-of-order batches, overlaps, REST/WS clock skew, the complete four-state table, authoritative regression, arbitrary reconnect batches, duplicate-only signals, adjacency appearing/disappearing across older and later pages, exact-1000 boundaries, missing-middle continuity, exact open-time lookup/index mapping, and checked oldest-minus-one without panic or reordering. Performance/representation tests prove same-key newest upsert and newest append select allocation-free `Identity`, eligible single prepend/middle shifts select `ShiftSuffix`, general nonuniform batches may select `Explicit`, every valid prior index maps to its final valid index, out-of-domain indices return `None`, and the append predecessor check consults the previous back element first without changing counts, authority, adjacency, resolved mutations, or ordering. Anchor preservation before/inside/after the viewport belongs only to chunk 5; the sole end/no-progress latch belongs only to chunk 16.

### 5. Implement pure chart view state and numerical invariants

- [x] Define `ChartViewState` logical states/initializers, including distinct empty-data state; `InteractiveChartState::LayoutPending` is App state owned by chunk 17, not a dummy chart view, while chunk 11 only supplies pure layout results.
- [x] Pure view initialization requires explicit positive `plot_width`; chunk 17 performs the sole first-adequate transition using chunk 11's `ChartLayoutResult::Ready`, and later resizes preserve/reclamp.
- [x] Retain exact snapshot and interactive count formulas, the exact nonempty interactive `min_visible_count = min(series_len, plot_width, 10)`, and the global plot-width invariant.
- [x] Implement visibility bounds, pan/zoom rounding, keyboard zoom anchors, live-follow/latest invariant, `End`, distinct `r`, live append, resize center preservation/re-clamping, and every index-aware series mutation. After every keyboard or mouse X zoom, enforce `min_visible_count` and then reclamp all visibility bounds; a further zoom-in at that minimum is a no-op.
- [x] On every series mutation, preserve persistent logical X/right-edge/center references through candle `open_time`, `MutationSummary.old_to_new.map(old_index)`, and `MutationSummary.resolved`; test equivalent anchor semantics for `Identity`, `ShiftSuffix`, and `Explicit`, including insertions before, inside, and after the viewport. Cancel active drag and set coordinate-derived hover to `None`; chunk 5 performs no retained-coordinate projection.
- [x] Implement overflow-resistant visible-extrema math with exactly 5% auto-Y padding on each side, deterministic flat/tiny fallback, finite projection for zero/negative/tiny/large values, and manual Y pan/zoom disabling auto-scale even when clamped.
- [x] Own the flat/tiny Y epsilon, minimum/maximum manual span formulas, checked exponent short-circuit, and every chart-state numeric boundary assigned by the canonical plan.
- Definition of done: pure tests with explicit plot widths prove exact initial counts, distinct empty state, exact 5% padding, every factor/sign/rounding/bound, 1–9 behavior, global plot-width ceiling after every mutation/zoom/resize, exact `min_visible_count = min(series_len, plot_width, 10)` after keyboard and mouse X zoom plus reclamping, and a blocked next zoom-in at that exact minimum, live follow, open-time/`IndexMapping` anchor preservation with equivalent observable results for `Identity`, `ShiftSuffix`, and `Explicit` on insertions before/inside/after the viewport, and post-mutation drag cancellation/hover invalidation. No layout, hit testing, retained-coordinate hover reprojection, or formatter integration is implemented in this chunk.

### 6. Implement CLI parsing and pure Binance canonicalization

- [x] Define positional `instrument`, `timeframe`, and `-i`/`--interactive`; parse generic provider prefix and pair grammar into `InstrumentSpec`, with explicit error precedence. Chunk 6 parses `ProviderId` only; concrete registry lookup/selection occurs only after full Binance REST/live implementations in chunk 10.
- [x] Reject malformed symbols/providers/timeframes before terminal, runtime, or network work; preserve case-sensitive `1m`/`1M` and exact intervals. Reject quote-only suffix tokens after canonical splitting; require nonempty canonical base and quote.
- [x] Implement Binance-local pure uppercase/canonical resolution and display/provider symbol fields without network access, including the shortest valid suffixed pair.
- [x] Use only `Cli::try_parse_from`/command rendering library tests. No Assert Cmd or binary path in chunk 6; binary help/version/argument errors begin only in chunk 17.
- Definition of done: parser/canonicalization library behavior only; valid modes are later direct `run_with_dependencies` tests, never child-process injection.

### 7. Define provider interface and task ownership

- [x] Implement exact chunk-7 aliases, provider traits, non-`Clone` `LiveRequest`, `LiveFeed`, `ProducerCompletionReceiver`, `RateGateSnapshot`, and every canonical latest-value endpoint/error symbol from the sole plan block. Keep `ReconcileAckReceiver` non-cloneable and sole: App retains `ReconcileAckSender`, while the consuming supervisor receives the receiver through `LiveRequest`.
- [x] Construct `accepted_watermark_channel(initial)` and initially-empty `reconcile_ack_channel()`; verify synchronous publication, awaitable `changed`, per-clone cursors, expectation registration, matching proof-only ack, every exact closure/publication/expectation error, and no polling.
- [x] Define cancellation, `Clock`, manual scheduler, checked duration-to-`MonoInstant` conversion, and task timeout seams; do not redefine chunk-3 deadline values.
- [x] Fake-provider tests prove one-way `ProducerCompletion::Running`→`Finished`, active non-consuming observation, and later consuming join returning the same result without duplicate accounting.
- [x] Preserve request-shutdown-before-restoration and consuming bounded join after restoration.
- Definition of done: exact canonical APIs and symbols compile with no aliases or invented channels; no concrete Binance/registry exists yet.

### 8. Implement deterministic Binance REST

- [x] Build private Binance foundations with exact production REST base `https://data-api.binance.vision`, injectable literal-loopback test base, one shared Reqwest client/gate, exact REST timeout/cap, the identifying `fccli` User-Agent, and production constructor boundary.
- [x] Implement exact `/api/v3/klines` latest/older/gap queries and checked pagination.
- [x] Decode validated 12-element arrays into immutable `RestProvisionalOpen` candles only.
- [x] Enforce exact matrix: invalid-symbol HTTP 400 sanitized Binance code/message; every other non-418/non-429 HTTP 4xx (including non-invalid-symbol 400 and 403) maps to the sanitized typed non-retryable HTTP/client-status error; 429 valid `Retry-After` or exact 30 s invalid/missing fallback; 418 valid deadline or absorbing `InvalidBanExpiry`; generic 5xx recoverable status; typed request timeout/transport failure; typed malformed JSON/non-array/wrong-arity/invalid-field/over-budget payload failure.
- [x] Add independent local Wiremock cases for every matrix row, including generic non-special 4xx/client status, plus exact endpoint/query, identifying `fccli` User-Agent, isolation, redirects/public-connect negatives, and production-constructor boundary assertions.
- Definition of done: every matrix row, exact endpoint/query, and identifying `fccli` User-Agent is independently covered; invalid-symbol 400 is distinguished from other 400 responses, generic client status is terminal/non-retryable, and no “original cases” reference is needed.

### 9. Implement the WebSocket codec and local harness

- [x] Decode private Binance kline payloads into a closed internal decoded-frame outcome covering validated open/closed candle data, ignorable/provider frames, close, `serverShutdown`, malformed/provider error, and terminal configuration/invariant failure. The codec does not construct `MarketEvent`, choose generation tags, transition connection status, set retry deadlines, or decide in-band terminal delivery; chunk 10 owns all such mappings.
- [x] Construct exact `wss://data-stream.binance.vision/ws/<lowercase-symbol>@kline_<timeframe>`; validate byte sizes, frame ≤ message, and strictly `write_buffer_size < max_write_buffer_size` before Tungstenite. Equality must return typed configuration error. Chunk 9 exclusively owns/validates `WS_STALLED_WRITE_TIMEOUT = 5 s` over `1 ms..=60 s`.
- [x] Continuously poll/flush so Tungstenite automatic Pong works; never manually duplicate it. Chunk 9 owns only raw socket behavior without the application channel: prove automatic Pong while ordinary application writes are stalled. The real bounded downstream queues, sender-readiness selection, and full-channel behavior belong only to chunk 10.
- [x] Add open/closed/malformed/serverShutdown/oversized fixtures and a loopback `TcpListener` + `accept_async` WebSocket harness (Wiremock remains REST-only).
- Definition of done: local codec tests cover every timeframe mapping, open/closed candle decoding and authority, field and configured-budget failures, close/serverShutdown internal outcomes, oversized fragmented input, raw-socket stalled writes, and exactly one automatic Pong per Ping without an application queue. Generation/status/retry/`MarketEvent`, full-downstream, and multiple-Ping progress tests belong only to chunk 10.

### 10. Implement the WebSocket supervisor and reconciliation state machine

- [x] Implement exact state transitions with cancellation dominant and construct every exact generation-tagged `MarketEvent`, including `Candle { generation, candle }`, from chunk-9 decoded-frame outcomes. During gap synchronization, every non-special HTTP 4xx from REST emits `MarketEvent::TerminalError` with the sanitized typed non-retryable HTTP/client-status `ProviderError` and terminates the producer; emit other `TerminalError` values only for unrecoverable provider configuration/invariants, and use terminal stream `Err` only after in-band delivery is impossible.
- [x] Every generation, including `Some(confirmed)`, waits for a first decoded WS kline under `FIRST_KLINE_HANDSHAKE_TIMEOUT` before REST reconciliation can complete; event-at-deadline wins. Every reconciliation WS candle mutation advances `revision` and sets `target_open_time = max(current_target_open_time, candle.open_time)`, including accepted or coalesced mutations between or during paginated REST requests. Register the exact latest `ReconcileExpectation` before each batch; matching proof-only ack of App continuity through that latest target/revision gates `Connected`.
- [x] Missing-middle no-ack reaches chunk-10-owned `RECONCILE_ACK_TIMEOUT = 10 s` (`1 ms..=60 s`, ack-at-deadline wins), emits exact `ProviderError::ReconcileAckTimeout`, invalidates/purges/closes, then enters Backoff. Retain exact gap no-progress and invalid-418 producer transitions.
- [x] Select over awaitable watermark/ack changes, read/flush, gate, work, and bounded sender readiness. Use exact production capacities `KEYED_CANDLE_CAPACITY = 1024`, `CONTROL_CAPACITY = 64`, `MARKET_EVENT_CHANNEL_CAPACITY = 256`, and non-configurable `EMERGENCY_CONTROL_CAPACITY = 2`; test-injectable non-emergency values validate only over `1..=65_536`, and normal traffic can never consume the two emergency slots reserved for the overflow error/`Backoff` pair. artifact://2081 passes the 23 codec tests and 42 live tests, including exact production constants and independent capacity bounds, sustained WS-versus-ready-REST ordering, authority-aware/coalesced target growth, capacity-one saturation while automatic Pong progresses, ordered reserved overflow error/`Backoff` delivery before retry, cancellation suppression of a queued emergency pair, receiver-drop unblocking, and stalled-write/control-frame progress.
- [x] Own and validate only those supervisor capacities, `FIRST_KLINE_HANDSHAKE_TIMEOUT = 10 s`, `RECONCILE_ACK_TIMEOUT = 10 s`, max age, and reconnect backoff `1, 2, 4, 8, 16, 30, 30, … s`. Reset backoff only after acknowledged `Connected`; each retry waits until `max(checked_backoff_deadline, current_rate_gate_deadline)`. Consume chunk-9 validated stalled-write timeout and own only recovery transition/integration. REST timeout is chunk 8; App quota/join timeouts chunk 17. artifact://2081 passes exact-deadline, full capped backoff, acknowledged-reset, rate-gate maximum, max-age, and stalled-write recovery coverage.
- [x] Complete concrete shared Binance registry only after producer behavior passes. artifact://2047 passes shared `Arc` identity, known/unknown lookup, trait-object canonicalization/history/rate-gate/`open_live`, completion observation plus consuming join, and both three-test `api_boundaries` feature-mode suites. `combined_production_constructors_are_unnameable` verifies registry construction together with both feature-forbidden `BinanceProvider` and WebSocket constructors in each compile fixture with independent missing-symbol diagnostics.
- Definition of done: manual-clock and boundary producer tests cover the advancing current-target barrier, WS mutations before/between/after multiple REST pages, a `1M` timeframe page-boundary race, App continuity required through the latest target/revision before `Connected`, expectation channel errors, no-ack timeout/equality, gap-sync non-special HTTP 4xx in-band `MarketEvent::TerminalError` delivery followed by producer termination, producer completion, every capacity bound and permanently reserved emergency pair, saturation, the full reconnect sequence/repeat/reset, rate-gate maximum interaction, multiple-Ping progress under full downstream capacity, and registry; stalled-write configuration validation remains solely chunk 9. Consumer handling and state-preservation proof belong solely to chunk 17.

### 11. Implement deterministic layout and formatting

- [x] Purely calculate retained layout from the actual frame origin/size. The sole canonical enum is exactly `ChartLayoutResult::LayoutPending { required: Size, actual: Size } | ChartLayoutResult::Ready { layout: ChartLayout }`: pending contains `required = Size { width: 60, height: 18 }` and the actual effective frame size, while ready contains only `layout`. No variant or `ChartLayout` stores a duplicate `plot_width`; every consumer derives it from `layout.main_plot.width`. Adequate input retains exactly `ChartLayout { frame, header, main_plot, volume, gutter, price_axis, utc_axis, footer }`, where `footer: Option<Rect>` and every other named field is a retained `Rect`. Chunk 11 only computes layout and related pure formatting/slot geometry: it does not render, construct `RendererSnapshot`, create a placeholder chart view, mutate `InteractiveChartState`, or perform the first-adequate transition.
- [x] For input `frame = Rect { x: x0, y: y0, width: W, height: T }`, return `LayoutPending { required: Size { width: 60, height: 18 }, actual: Size { width: W, height: T } }` whenever effective `W < 60 || T < 18`, before evaluating any reservation equation. Otherwise let `header_h = 2`, `utc_h = 1`, `footer_h = if interactive { 1 } else { 0 }`, `gutter_w = if W >= 2 { 1 } else { 0 }`, `price_w = min(14, W.saturating_sub(gutter_w + 1))`, `plot_w = W - gutter_w - price_w`, and checked/saturating `H = T - header_h - utc_h - footer_h`; the adequate-frame guard guarantees `H >= 4`. Let `volume_h = clamp((H + 2) / 5, 3, H - 1)` and `main_h = H - volume_h`. The exact retained rectangles are `frame = input`, `header = Rect(x0, y0, W, 2)`, `main_plot = Rect(x0, y0 + 2, plot_w, main_h)`, `volume = Rect(x0, y0 + 2 + main_h, plot_w, volume_h)`, `gutter = Rect(x0 + plot_w, y0 + 2, gutter_w, H)`, `price_axis = Rect(x0 + plot_w + gutter_w, y0 + 2, price_w, H)`, `utc_axis = Rect(x0, y0 + 2 + H, plot_w, 1)`, and interactive-only `footer = Some(Rect(x0, y0 + 2 + H + 1, W, 1))`; snapshot uses `footer = None`.
- [x] Freeze universal half-open containment and hit regions from those exact retained rectangles: `main_plot` alone owns candle hover and left plot-pan drag; the full `price_axis` alone owns Y-scale drag; `utc_axis` alone owns X-scale drag; `header`, `volume`, `gutter`, and `footer` are noninteractive; any coordinate outside `frame`, on a rectangle's right edge, or on its bottom edge is outside that region. Own the sole `CandleSlotGeometry` half-open equal-partition/remainder-left/center-floor formula and tests for renderer and interaction consumers.
- [x] Implement only pure price/volume/UTC formatting and retained layout/slot projection helpers; no Buffer rendering or App/view-state transition belongs here.
- Definition of done: pure tests prove exact `LayoutPending { required, actual }` values for effective 59×18 and 60×17 frames, `Ready { layout }` for effective 60×18, exactly the canonical `ChartLayout` fields, exact adequate rectangles/consumer-derived `layout.main_plot.width`, and universal half-open hit regions at nonzero origins and fixed 60×18/80×24/120×36 frames, the pre-equation minimum guard, `H >= 4`, the complete reservation equations, exact volume/main split, formatting, and sole slot geometry. No competing layout result/type, duplicate stored plot width, dummy empty/zero-width view, renderer snapshot construction, resize-only rendering, first-adequate App transition, or interactive state mutation exists in chunk 11. Verification: artifact://2156 records 89 default-feature tests and 177 test-transport tests passing, with `cargo check` and Clippy clean.

### 12. Implement the shared custom Buffer renderer and overlays

- [x] Implement one Ratatui widget writing directly into `Buffer`, with public input `RenderPolicy`; do not use `cli-candlestick-chart`, Ratatui `Canvas`, or another candlestick widget. Verified by `ChartWidget`/`RenderPolicy` in `src/chart/widget.rs`, the fixed-`Buffer` renderer tests, and artifact://2326.
- [x] Project candles with the complete style-free wick/body-edge glyph inventory `│`, `┃`, `╷`, `╵`, `╻`, `╹`, `╽`, `╿`, plus bull body `█`, bear body `▓`, and doji `━`. Name and implement the exact single-row/two-row wick-only, wick-to-body edge, body-interior, clipped top/bottom wick, body/wick overlap, and doji projection cases; use chunk 11's sole `CandleSlotGeometry` for centered wicks and repeated bodies/directional volume, never aggregate source candles, and enforce `visible_count <= plot_width`. Verified by the exact per-slot/per-row projection table, clipping/offscreen, one-column/multi-column, zero-volume, full-inventory, shared-geometry, and plot-width ceiling tests in artifact://2326.
- [x] Freeze grid drawing before candles/overlays: each horizontal price-tick row writes glyph `─` with `Style::default().fg(Color::DarkGray)` across every `main_plot` cell in color policy and glyph `─` with exactly `Style::default()` in style-free policy; no vertical grid is drawn. Candles and volume then overwrite grid cells they occupy. Verified by the fixed-`Buffer` style/overwrite table and color/style-free grid tests in artifact://2326.
- [x] Introduce and own canonical `RendererSnapshot` with `chart_state` and no `layout_result` field; no `RendererFrameState` exists. The renderer receives a complete `RendererSnapshot` and a separate canonical `ChartLayoutResult` argument. Render `ChartLayoutResult::LayoutPending { required, actual }` as resize-only with no chart rectangles, and render `ChartLayoutResult::Ready { layout }` as the chart; consumers derive plot width only from `layout.main_plot.width`, without requiring a placeholder view or performing initialization. Preserve exact status/header/footer contracts. The `InteractiveChartState::LayoutPending`→`Ready` first-adequate transition remains exclusively in chunk 17. Verified structurally and by the resize-only, ready-layout, snapshot-header, and state/layout mismatch tests in artifact://2326.
- [x] Render crosshair last and never in `volume`, `header`, or `footer`: vertical glyph `┆`, horizontal glyph `┄`, and their intersection glyph `┼`; color policy uses `Style::default().fg(Color::Yellow)` for all three, while style-free policy uses the identical glyphs/order with exactly `Style::default()` and no ANSI. The vertical segment covers `main_plot` only. The horizontal segment replaces grid/candle cells across `main_plot` and continues as `┄` through `gutter`. The crosshair intersection is `┼` and wins over both segments. Verified for both policies by the exact style/overwrite table, intersection/gutter/extent assertions, hover-clear, and snapshot-hover suppression tests in artifact://2326.
- [x] Axis overlays are final: center the complete UTC label on the hovered candle center, then clamp the whole label inside `utc_axis`; it replaces underlying UTC ticks. Left-align the complete Y-price label and space-pad it to exactly `price_axis.width`; it replaces the entire corresponding `price_axis` row. Both labels use `Style::default().fg(Color::Black).bg(Color::Yellow)` in color policy and exactly `Style::default()` in style-free policy, remain clipped to their retained axis rectangles, and never spill into gutter/frame. When `hover = None`, including after an outside-plot move or chunk-5 mutation invalidation, no crosshair/axis overlay remains and the two-line header shows latest-candle details rather than stale hovered OHLCV. Verified by exact left/right clamp, complete-label, padding/replacement, clipping, hostile-cell reset, and latest-header assertions in artifact://2326.
- [x] Apply only `RenderPolicy`; test exact green/red candle and volume styles, the frozen grid/crosshair/axis-overlay styles, and every style-free glyph `│`, `┃`, `╷`, `╵`, `╻`, `╹`, `╽`, `╿`, `█`, `▓`, `━`, `─`, `┆`, `┄`, and `┼` regardless of `NO_COLOR`. Verified by the policy-specific full-inventory, hostile-cell reset, exact complete-style, directional candle/volume, grid, crosshair, and axis-overlay assertions in artifact://2326; rendering consults only the explicit `RenderPolicy` input and not the environment.
- Definition of done: pure fixed-`Buffer` tests pass a canonical already-reduced `RendererSnapshot` and separate `ChartLayoutResult` argument and assert exact glyphs and complete `Style` values for grid-before-candle, crosshair-over-grid/candle, `┼` intersection, horizontal `┄` gutter continuation, UTC whole-label centering/clamping, Y-label left alignment/space padding/full-row replacement, color and style-free policies, nonzero origins, and unchanged cells outside retained rectangles; they also cover every wick/body-edge/body/doji projection case, exact layout/time/empty formulas, exact `SNAPSHOT`, tiny/large price precision, compact volume bounds, directional volume, plot-width ceiling, hover-clear/latest-header reversion, and canonical statuses. `RendererSnapshot` contains `chart_state` and no `layout_result`; no `RendererFrameState` or stale-generation/control/candle reduction belongs in the renderer.
Verification evidence: artifact://2326 passes the main default-feature suite (129 tests), test-transport suite (217 tests), all 34 `chart_render` tests, `cargo check --locked --all-targets`, and `cargo clippy --locked --all-targets -- -D warnings`.

### 13. Implement keyboard and mouse interaction mapping

- [x] Map press/repeat events for every locked key; ignore releases to prevent Windows duplicates; quit returns an action to the common shutdown path rather than exiting directly. Verified by the exhaustive key table and press/repeat/release/ignored-key/deferred-quit assertions in `tests/chart_interaction.rs`, the closed `InteractionAction` mapping in `src/chart/interaction.rs`, and artifact://2407.
- [x] Map hover to nearest candle/price through the permanent chunk-11 `ChartLayout` rectangles and sole `CandleSlotGeometry`, with half-open containment and earlier/left ties; an unowned mouse move outside the plot emits explicit hover clear. Retain pointer coordinates, left-button drag ownership, starting state, initial cursor-candle X anchor, and initial-row mapped-price Y anchor. Verified by the shared-slot half-open hover, outside clear, retained pointer, fixed ownership, anchor, nonzero/nondivisible geometry, release, and mutation reprojection assertions in `tests/chart_interaction.rs`, together with the permanent slot-geometry boundary tests and artifact://2407.
- [x] Implement plot X/Y content-following pan, with downward plot drag raising the viewed price center; time-axis zoom around the initial cursor candle; price-axis zoom around the initial button-down price; fixed ownership outside the region; frame clamping; and mouse-up commit. Every mouse X zoom applies the chunk-5 nonempty `min_visible_count = min(series_len, plot_width, 10)` and then reclamps all visibility bounds; another zoom-in at that minimum is blocked. Chunk 13 exclusively owns coordinate projection but never a second slot partition: ordinary movement and release use the chunk-11 helper to compute or clear hover, and after chunk-5 mutation invalidation the next reducer pass may reuse it when retained coordinates remain inside the new plot. Verified by exact `1.05`-power/sign assertions, plot content-following, initial X/price anchors, frame clamping, fixed outside ownership, drag hover suppression, release projection/clear, real-series mutation cancellation/reprojection, all three X-floor limiters, reclamping, and blocked-next-zoom assertions in `tests/chart_interaction.rs` and artifact://2407.
- Definition of done: synthetic platform-neutral tests prove exactly one action, every sign/factor/rounding, region distinction, downward-drag content following, X/price anchors, suppression during drag, inside→outside clearing, owned outside movement, release projection/clear, post-mutation retained-coordinate reprojection only through the permanent chunk-11/13 path, and mouse X zoom reaching the exact chunk-5 minimum, reclamping, and blocking the next zoom-in. Exact gaps: none; source inspection matches the canonical interaction path and artifact://2407 passes all 12 exact `chart_interaction` tests, the 141-test default-feature suite, the 229-test test-transport suite, `cargo check --locked --all-targets`, and `cargo clippy --locked --all-targets -- -D warnings`.

### 14. Implement snapshot output

- [x] Expose and directly test the permanent injected snapshot runner as the library-only `snapshot::run_snapshot` API. It requests `HistoryRequest::latest(..., limit=500)`, and `non_tty_requests_latest_500_and_writes_one_plain_fixed_frame` asserts the exact latest limit `500`. Chunk 14 adds no Assert Cmd, binary help/version/invalid, dispatcher, `RunDependencies`, or `run_with_dependencies`. Verified structurally in `src/snapshot.rs`, directly by all 6 exact `tests/snapshot_runner.rs` tests, and by artifact://2509.
- [x] Preserve exact TTY inline sizing behavior: `SnapshotOutputTarget::Tty::effective_size` reserves one physical shell row before pure layout; `tty_reserves_shell_row_at_exact_layout_boundary` proves physical 60×18 becomes effective 60×17 and returns the resize-only `LayoutPending` frame without requesting history, while physical 60×19 becomes effective 60×18 and returns `Ready { layout }` with an exact 60×18 inline frame. Verified by artifact://2509.
- [x] Preserve exact non-TTY 120×36 serialization. `SnapshotOutputTarget::NonTty` uses `NON_TTY_SNAPSHOT_SIZE = 120×36`; the exact runner test proves one plain style-free fixed frame containing the snapshot market identity, exactly 36 newline-terminated rows, and exactly 120 display cells per row. Verified by artifact://2509.
- [x] Preserve exit/error semantics: non-TTY color is rejected before provider/output work; provider errors remain typed; caller-owned cancellation stops pending history without output; output failures are typed and sanitized; TTY color is deterministic, resets styles, and emits no raw/alternate-screen/mouse/cursor mode sequences. Verified by the exact error, cancellation, output-failure, and TTY-color runner tests in artifact://2509.
- Definition of done: complete. The permanent library-only real snapshot path and renderer behavior are covered, including the physical TTY 60×18 fail/60×19 pass boundary; all binary early-exit coverage remains deferred to chunk 17. Exact gaps: none. Artifact://2509 passes the 148-test default-feature suite, the 236-test test-transport suite, all 6 exact `snapshot_runner` tests in both feature modes, `cargo check --locked --all-targets`, and `cargo clippy --locked --all-targets -- -D warnings`.
- Final reviewer autowrap finding: discarded; no source or test change is required. Main-delegated real Linux PTY evidence used tmux 3.4 with `TERM=xterm-256color` in an exact 60×19 pane and wrote 18 full-width 60-cell rows with CRLF only between rows. All 18 rows remained contiguous with no blank/double-spaced rows; after row 18 the cursor was at the wrap-pending right margin (`x=60`, zero-based `y=17`), and the next printable byte appeared at row 19 column 1 (`x=1`, zero-based `y=18`). This directly confirms the existing one-spare-row/no-final-newline design and disproves the alleged prompt-overwrite/autowrap defect. Chunk 14 remains complete on artifact://2509 evidence.

### 15. Implement transactional `TerminalSession`

- [x] Separate session mechanics from `App`: `src/terminal.rs` exposes the non-generic injected `TerminalSession` transaction, which owns an `Arc<dyn TerminalDriver>`, and the production `CrosstermTerminalDriver::enter_checked`, which validates both process stdin and stdout as TTYs before the first mutation. The mandatory Linux-only Nix 0.30.1 (`term`, `process`, `fs`) `openpty`/spawn test exercises the production driver and real kernel state; deterministic faults use an injected `Arc<dyn TerminalDriver>`. CLI validation, initial REST success, and the call to `enter_checked` remain the chunk-17 caller-ordering responsibility rather than being coupled into the session.
- [x] Track each raw/alternate/mouse/cursor setup step as `NotAttempted`, `AttemptedOrActive`, or `Restored`, marking it attempted before issuing the mutation so side-effect-then-error remains rollback-eligible; inverse commands are idempotent best effort. `setup_and_restore_use_exact_transactional_order_and_states` and `every_setup_failure_before_or_after_side_effect_rolls_back_every_uncertain_step` cover the exact state/order contract.
- [x] Explicit teardown is non-short-circuiting: attempt every undo in reverse order, aggregate sanitized failures, mark only successful restoration, and retain a preexisting application/provider/render error as the returned primary error with cleanup aggregate secondary. An otherwise-successful terminal finish returns cleanup as the primary error for the later dispatcher to map to nonzero status. Best-effort non-panicking `Drop` retries uncertain state, catches inverse panics, and does not replace an existing unwind. Deterministic tests cover later undos, retry, provider/render precedence, cleanup-primary terminal finish, and panic/unwind behavior.
- [x] Inject pre-side-effect and side-effect-then-error for every setup plus teardown failures; terminal finish independent of its upstream shutdown cause, panic cleanup, sanitization, and synchronous restoration/close/cancel/join callback ordering are covered. `DET-LINUX-PTY` (artifact://2629) records that the exact PTY test ran rather than being zero-filtered, proved raw mode was actively applied before restoration, restored complete slave termios/kernel echo/raw state, emitted setup/inverse mode sequences in order, and returned the provider primary error after cleanup. The same retained evidence proves the caller-owned production driver enters `TerminalSession` through the identical `Arc<dyn TerminalDriver>` allocation rather than reconstructing or substituting the driver.
- Definition of done: complete for chunk 15. Source inspection finds no active-session `process::exit`; exact deterministic tests prove setup/reverse teardown, every injected setup/teardown fault path, non-short-circuit restoration, returned-error precedence, cleanup-primary signaling, non-panicking retrying `Drop`, panic unwind, cause-agnostic terminal finish restoration, synchronous restoration before close/cancel/join callbacks, active raw-state application/restoration, and the same-`Arc` production-driver boundary. `DET-LINUX-PTY` (artifact://2629) records the exact PTY result; the same retained evidence also passes `cargo check --locked --all-targets`, the 167-test default-feature suite, the 255-test test-transport suite, all 19 discovered `terminal_lifecycle` tests (16 PASS and the 3 manual real-TTY inspection probes intentionally ignored), and `cargo clippy --locked --all-targets -- -D warnings`. All four chunk-15 mechanics items remain checked; real `q`/`Esc`/Ctrl-C caller integration remains deferred to chunk 17, and hosted-terminal/manual restoration rows remain governed only by the authoritative ledger.

### 16. Implement history coordination after store and view state

- [x] Implement the sole end/no-progress history latch in the pure `HistoryCoordinator`, which tracks its in-flight task, current observable rate-gate handle, retry deadline, one deferred arm, process-blocking 418 status, persistent typed non-retryable-client-4xx no-further-history-request state, last error/status, cancellation, and boundary state. `CandleSeries` and `ChartViewState` retain no duplicate history latch or failure state. Verified by source inspection and the exact history coordinator suite in artifact://2707.
- [x] Set `threshold = max(1, ceil(loaded_len × 0.10))`; trigger only when visible-left source index crosses to `< threshold`, never for empty data, in flight, before the current observed shared-gate deadline, after the coordinator's end/no-progress latch, while process-blocking 418 status is active, or after entering the typed non-retryable-client-4xx no-further-history-request state. Every wake rereads the provider-neutral gate observer, and observer closure becomes terminal typed state. Exact threshold, single-flight, boundary crossing/disarm, timed-gate extension, process block, and observer-closure behavior are covered by artifact://2707.
- [x] Serialize requests and consume `MutationSummary`: empty-input, duplicate-only, or a result with no actual older insertion sets the sole latch; actual older insertion clears/re-evaluates it only for the newly accepted request result. A 429 or valid-deadline 418 arms exactly one retry at the current gate while still inside; concurrent extension moves it, moving outside disarms it, and cancellation suppresses it. Invalid/missing 418 records blocker status and permanently disables automatic history retry for this process. Only typed recoverable failures permit a later retry or boundary re-crossing. A typed non-retryable client HTTP 4xx enters the persistent no-further-history-request state, preserves every accepted candle and the current view, and never issues another history request. The 30-second production 429 fallback is covered at the shared REST gate boundary, while the coordinator's consumption of the resulting observed deadline is covered by the history tests; artifact://2707 covers both feature modes.
- [x] Apply accepted history pages through `ChartViewState::apply_mutation` using canonical `MutationSummary.old_to_new.map(old_index)`/`resolved` anchors and the global `visible_count <= plot_width` clamp; drag cancellation and coordinate-derived hover clearing remain owned by `ChartViewState`. The coordinator owns neither anchors nor hover reprojection. History integration is covered by artifact://2707; the exact anchor/mapping behavior remains the already-completed chunk-5 chart-state coverage rather than duplicated history tests.
- Definition of done: complete for chunk 16. Exactly 20 discovered `tests/history_coordinator.rs` tests pass in both feature modes and prove single flight; the sole empty/duplicate/non-older no-progress latch; checked oldest-minus-one and limit 1000; valid 418 and shared timed-gate retry; typed recoverable-only retry; persistent non-retryable client 4xx with accepted series/view preserved; current-gate extension/non-shortening; observer closure; boundary disarm/re-crossing; mutation application; underflow safety; cancellation and dropped-drive ownership; panic sanitization/control precedence; and absence of request loops. Exact gaps: none within chunk 16; anchor projection remains intentionally owned and already verified by chunk 5. Artifact://2707 passes the 187-test default-feature suite, the 275-test test-transport suite, all 20 exact history coordinator tests in each feature mode, `cargo check --locked --all-targets`, and `cargo clippy --locked --all-targets -- -D warnings`. Chunk 17 and later remain unchecked.

### 17. Implement the asynchronous interactive application loop
- [x] Prove real interactive shutdown integration: `q`, `Esc`, and Ctrl-C route through the App shutdown path; successful initial REST completes before `CrosstermTerminalDriver::enter_checked`; and a genuinely hung producer is bounded by the App join deadline while terminal restoration completes before the join wait. `direct_interactive_dispatch_fetches_before_terminal_and_restores_on_q`, `esc_and_ctrl_c_share_the_graceful_shutdown_path`, and `hung_producer_is_join_bounded_after_terminal_restoration` provide the chunk-17 App/caller acceptance proof in artifact://3134; these are not credited to chunk-15 `TerminalSession` mechanics.

- [x] Build interactive App, then sole `RunDependencies`/`run_with_dependencies`/main dispatch. The implemented boundary is `RunDependencies.input: Box<dyn TerminalInput>` where `TerminalInput: Send` exposes finite `poll(&mut self, timeout: Duration) -> io::Result<TerminalInputPoll>` with `TerminalInputPoll::Event(Event) | Idle | Closed`. App moves that input into one owned `InputReaderTask`; the task polls with `INPUT_POLL_TIMEOUT`, observes App cancellation between polls, and is definitively joined before terminal restoration. The timeout bounds each poll and therefore cancellation latency; it is not a separate timed-join contract. The superseded raw `Box<dyn Read + Send>` plus App-local byte/ANSI decoder design remains withdrawn: production decoding is supplied by `CrosstermTerminalInput`, injected implementations supply complete Crossterm `Event` values, and no detached or uninterruptible reader thread is permitted. Assert Cmd remains restricted to help/version/argument-error paths before dependencies; valid modes call `run_with_dependencies` directly with local dependencies. `tests/app_live_contract.rs` is the sole live App integration target and contains exactly 40 discovered tests in artifact://3134.
- [x] App owns `InteractiveChartState::LayoutPending | Ready`, latest-value senders, reconciliation keys, status and renderer snapshot, and publishes only proof-valid matching acknowledgements. The renderer cache rebuilds only for accepted state changes; terminal-size sampling failures enter the typed terminal-failure/cleanup path rather than rendering stale geometry. The direct reconciliation/state-persistence contract is covered in the 40-test App suite in artifact://3134 and additionally isolated by artifact://3136 (`reconciliation_target_and_state_persistence`: 1 passed, 39 filtered out).
- [x] Undersized startup remains `InteractiveChartState::LayoutPending`; chunk 17 exclusively consumes the first adequate `ChartLayoutResult::Ready` to initialize once, while later resizes preserve/reclamp and never reinitialize. Redraw is change-driven. Initial, periodic, and explicit-resize terminal-size failures remain typed and restore/clean up in canonical order. Status transitions, reconnect, reconciliation/backfill, history terminal/nonfatal-client outcomes, and rate-limit waits/errors preserve accepted candles and the inspected viewport; only accepted series mutations use the canonical anchor-preserving update. Artifact://3134 covers completed and pending history pages, in-App coordinator execution, permanent history client 4xx, one-shot fatal history-task panic, maximum-clock cleanup, reconciliation/state persistence, rate-gate behavior, LayoutPending recovery, and renderer-cache behavior; artifact://3136 repeats the exact reconciliation aggregate.
- [x] Reducer uses `ProducerCompletionReceiver` for the active producer-completion bucket and preserves the canonical quota, stale-generation filtering, intra-epoch order, terminal short-circuit, reconciliation-before-candle order, generationless saturation order, and non-duplicated successful completion behavior. Generationless emergency saturation invalidates only the candidate generation, rejects delayed invalidated-generation events until a higher generation is accepted, and preserves canonical `QueueSaturated` then `Backoff` ordering across repeated cycles. The shared `RateGateSnapshot` is a quota-controlled reducer source: deadlines never shorten, `ProcessBlocked` is absorbing, closure is terminal, and displayed `TimedUntil` expires at exact injected-clock equality unless extended. Artifact://3134 covers the reducer/emergency/completion/rate-gate matrix and the real source-ready task predicate through the exact per-source epoch quota assertion.
- [x] Shutdown cancels and definitively joins the owned bounded-poll `InputReaderTask` before terminal restoration, then consumes bounded network/history joins; already-reduced producer completion is not duplicated. Artifact://3134 proves normal shutdown, input-channel closure, dropping the interactive future, and owned input drop all terminate the input task before terminal restoration; pending history work is aborted after restoration at the injected deadline; retained ready history pages clear without deadline overflow; producer success/stream completion exits exactly once; and genuinely hung producer/history tasks remain bounded without leaks.
- Definition of done: complete for chunk 17. Exactly 40 discovered `tests/app_live_contract.rs` tests pass under `test-transport` and cover direct snapshot/interactive dispatch; early-exit-only Assert Cmd; bounded `TerminalInput` polling, input-owner drop, future-drop cancellation, definitive task join, and no leaked input task; `q`/`Esc`/Ctrl-C shutdown; initial REST before terminal entry; terminal restoration before bounded producer/history joins; typed initial/periodic/explicit-resize size failures; history-page readiness, nonfatal permanent client 4xx, fatal task panic, shutdown, maximum-clock cleanup, and retained-task ownership; change-only renderer-cache rebuilds; advancing current-target/revision acknowledgement and timeout flows including multi-page/`1M` reconciliation; terminal gap-sync client 4xx consumption without retry; exclusive first-adequate layout transition and pending recovery; accepted-series/inspected-viewport preservation across history/status/reconnect/rate-gate paths; monotonic and exact-expiry rate-gate observation with terminal closure; reducer quotas/order/stale filtering/terminal short-circuit and real task-ready source predicate; generationless emergency invalidation and repeated saturation ordering; and producer completion/closure semantics. Exact gaps: none within chunk 17. Artifact://3134 is the authoritative complete chunk-17 gate evidence: formatting and Cargo check passed, the default-feature suite passed 190 tests, the test-transport suite passed 318 tests, all exact 40 App tests passed, and the final Clippy stage passed. Artifact://3136 separately passes the exact `reconciliation_target_and_state_persistence` test (1 passed, 39 filtered out). All six chunk-17 checklist items are complete. Chunk 18 and later work remain unchecked.

### 18. Finalize errors, compatibility, and deterministic isolation

- [ ] Stabilize actionable errors and exit semantics. Keep UI display precedence separate from returned-error precedence: preexisting application/provider/render error remains primary with teardown aggregate secondary; cleanup failure after successful quit is primary nonzero. Preserve Clap and snapshot exit rules; sanitize payloads/secrets.
- [ ] Detect `NO_COLOR` once into `RenderPolicy`; every style-free path forces directional monochrome.
- [ ] Ensure cross-platform input/TLS and no API keys.
- [ ] Keep fixtures/helpers with consuming features, including the combined non-vacuous REST/WS/provider production-constructor compile-fail boundary introduced alongside the real constructors, and formatter-safe timestamp/tiny-price/large-price/large-volume fixtures.
- Definition of done: failures are stable/no-panic; isolation and dependency/source review confirm no excluded behavior, hidden seam, or compatibility code.

### 19. Add CI and complete final verification

- [ ] Track deterministic verification only by these authoritative plan-ledger record IDs: `DET-FMT`, `DET-TEST-LINUX`, `DET-CLIPPY-LINUX`, `DET-TEST-MACOS`, `DET-CLIPPY-MACOS`, `DET-TEST-WINDOWS`, `DET-CLIPPY-WINDOWS`, `DET-PRODUCTION-CHECK`, `DET-LINUX-PTY`, `DET-DEFAULT-FEATURE`, `DET-MUTUAL-EXCLUSION`, `DET-CONSTRUCTOR-BOUNDARY`, and `DET-RECONCILIATION-STATE`.
- [ ] Track environment-dependent and final verification only by these authoritative plan-ledger record IDs: `REAL-SNAPSHOT`, `REAL-INTERACTIVE`, `REAL-INTERACTIVE-MOUSE`, `RESTORE-Q`, `RESTORE-ESC`, `RESTORE-CTRL-C`, `RESTORE-PROVIDER-ERROR`, `RESTORE-RENDER-ERROR`, `RESTORE-PANIC`, `TERM-LINUX`, `TERM-TMUX`, `TERM-MACOS-TERMINAL`, `TERM-MACOS-ITERM2`, `TERM-WINDOWS`, `MOUSE-LINUX`, `MOUSE-TMUX`, `MOUSE-MACOS-TERMINAL`, `MOUSE-MACOS-ITERM2`, `MOUSE-WINDOWS`, and `FINAL-SCOPE`.
- Definition of done: `FINAL-SCOPE`.
